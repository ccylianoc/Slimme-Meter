{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run Server with SQLite",
            "request": "launch",
            "mainClass": "Server",
            "vmArgs": "--enable-native-access=ALL-UNNAMED",
            "classPaths": [
                "${workspaceFolder}",
                "${workspaceFolder}/libs/sqlite-jdbc-3.51.1.0.jar"
            ]
        }
    ]
}
